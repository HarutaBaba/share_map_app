<% if @user != nil %>
  <% @user.followers.each do |user| %>
    <%= user.name %>
    <%= link_to '詳細', user_path(user) %>
  <% end %>
<% end %>

<% if @user == nil %>
  <p>フォロされているユーザーは存在しません。</p>
<% end %>